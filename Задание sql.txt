------------1
CREATE TABLE SHEDULERS AS 
(
	FIO varchar(255),
	SHEDULE varchar(255),
	DATE_BEGIN DATETIME,
	DATE_END DATETIME,

	CONSTRAINT AK_SHEDULERS UNIQUE(FIO, DATE_BEGIN),
	CHECK (DATE_BEGIN <= DATE_END) 
);

BULK INSERT SHEDULERS
FROM '\\shedulers.csv'
WITH
    (
    FIRSTROW = 2,
    FIELDTERMINATOR = ' ', 
    ROWTERMINATOR = '\n',   
    TABLOCK
    );

------------2
CREATE TABLE WORK_DAYS AS 
(
	FIO varchar(255),
	DATE_BEGIN DATETIME,
	DATE_END DATETIME
);

------------3
CREATE PROCEDURE CALCULATION_OF_WORKING DAYS
FIO varchar(255),
	DATE_BEGIN DATETIME,
	DATE_END DATETIME
AS


Не понятно как эту часть реализовывать .. Вам нужна процедура или функция которая принимает одну строку или принимает две даты как описано 

------------4
SELECT FIO , COUNT(DATE_BEGIN) AS COUNT_WORKDAY
FROM WORK_DAYS 
GROUP BY FIO ;

SELECT FIO , COUNT(DATE_BEGIN) AS COUNT_WORKDAY
FROM WORK_DAYS 
GROUP BY FIO 
HAVING COUNT_WORKDAY > 10;

SELECT FIO 
FROM WORK_DAYS 
WHERE FORMAT(DATE_BEGIN, 'DD.MM.YYYY') BETWEEN '14.01.2019' AND '16.01.2019'  ;